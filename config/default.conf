server {
  listen 127.0.0.1:8000;
  listen 127.0.0.1:9002;
  listen 127.0.0.1:9005;
  server_name www.google google;

  root www;

  cgi .php php-cgi;

  autoindex on;

  location /data {
    limit_except POST HEAD;
    index index.htm;
  }

  location /home {
    auth admin:admin;
    index index.html index.php;
  }

  location /data/hava {
    index index.htm index.html;
  }

  location /data/img {
    root /data/img;
  }
}
